# Tests

## Local CLI Testing

```shell
# Create tmp directory
mkdir tmp
cd tmp

# Setup new project
npm init

# Install package
npm install ../



```

1. Create ./tmp directory
2. Run `npm init`
3. Install `git-secrets` package by running 