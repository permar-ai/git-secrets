{
  "name": "git-secrets",
  "version": "1.0.0",
  "description": "Management of secrets directly in git",
  "author": "Matteo Berchier",
  "license": "MIT",
  "main": "./dist/index.js",
  "module": "./dist/index.mjs",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsup && npm run build:copy",
    "build:copy": "copyfiles -u 1 'src/*.sql' dist/",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "copyfiles": "^2.4.1",
    "ts-node": "^10.9.2",
    "tsup": "^8.3.0",
    "typescript": "^5.6.2"
  },
  "files": [
    "dist/",
    "sql/"
  ],
  "dependencies": {
    "better-sqlite3": "^11.3.0",
    "openpgp": "^5.11.2",
    "uuidv4": "^6.2.13"
  }
}
